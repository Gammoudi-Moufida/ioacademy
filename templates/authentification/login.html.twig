<div class="modal fade" id="LoginModal" tabindex="-1" aria-labelledby="LoginModal" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-body">
                <div class="modal-close">
                    <button type="button" class="btn-close float-end" data-bs-dismiss="modal"
                        aria-label="Close"></button>
                </div>
                <div class="text-center">
                    <h3 class="my-3">Connexion</h3>
                </div>
                <div class="container-fluid">
                    <form name="loginForm" id="loginForm" action="{{ path('login') }}" method="post" novalidate>
                        <div id="registration_result"></div>
                        <div class="mb-3">
                            <label class="form-label">Adresse email</label>
                            <input type="email" class="form-control" name="_username" id="login_email"
                                placeholder="Adresse email*" required="">
                        </div>
                        <div class="mb-3">
                            <div class="d-flex justify-content-between align-items-center">
                                <label class="form-label">Mot de passe</label>
                                <a class="mb-2 fw-bold nav-link p-0" href="javascript:;" role="button"
                                    data-bs-toggle="modal" data-bs-target="#ForgotPasswordModal">Mot de passe
                                    oublié?</a>
                            </div>

                            <input type="password" class="form-control" name="_password" id="login_password"
                                placeholder="Mot de passe*" required="">
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary">Connexion</button>
                        </div>
                    </form>
                </div>
            </div>
            <div class="modal-footer d-block text-center">
                <p>Vous n'avez pas encore de compte?
                    <a class="fw-bold nav-link p-0" href="javascript:;" role="button" data-bs-toggle="modal"
                        data-bs-target="#SignUpModal">S'inscrire</a>
                </p>
            </div>
        </div>
    </div>
</div>